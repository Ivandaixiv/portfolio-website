import { createStyles } from "@material-ui/styles";
const styles = createStyles({
  name: {
    fontSize: "5rem",
    textShadow: "5px 2px 10px #151515",
  },
  text: {
    textShadow: "5px 2px 10px #151515",
    fontWeight: 500,
    fontSize: "1.5rem",
    width: 500,
  },
});
export default styles;
