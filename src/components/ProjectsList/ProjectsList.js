import React from "react";
import { withStyles } from "@material-ui/core/styles";
import styles from "./styles";
const ProjectsList = ({ classes, data, events }) => {
  return (
    <ul>
      <li>Yes</li>
    </ul>
  );
};

export default withStyles(styles)(ProjectsList);
